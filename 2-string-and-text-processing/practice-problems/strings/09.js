// Call the language.slice method with arguments 1
// and 4. Next, call the method with arguments 2 and
// 4. Log the return values.

const language = 'JavaScript';
console.log(language.slice(1, 4))
console.log(language.slice(2, 4))
