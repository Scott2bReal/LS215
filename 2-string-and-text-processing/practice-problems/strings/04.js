// Create a variable named language and set it equal
// to 'JavaScript'. Find the index of the first
// occurrence of the letter 'S' and save it to a
// variable named idx variable. Log the value of
// idx.

const language = 'JavaScript';
const idxVariable = language.indexOf('S')
console.log(idxVariable)
